#include <stdio.h>


int main(){
    // Operadores Relacionais
    // > "Maior que"
    // >= "Maior ou igual"
    // < "Menor que"
    // <="Menor ou igual"
    // == "Igual a"
    // != "Diferente de"
    // = atribuição == comparação de igualdade

    int a = 5, b = 10;
    if (a >= b){
        printf("É maior");
        } else{
            printf("Num é");
        }

    getchar();
    return 0;
}
